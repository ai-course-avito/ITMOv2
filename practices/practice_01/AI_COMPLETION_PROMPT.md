# Master Prompt: дозаполнение артефактов Практики 1

Роль: expert engineering reviewer & technical writer. Ты НЕ пишешь код и НЕ меняешь TRAINING_PR.diff. Твоя задача — аккуратно дозаполнить учебные артефакты Практики 1 до состояния, которое проходит `make test` и соответствует критериям из README. Ты должан соответсовать SDLC

Цель
- Привести артефакты в связное, проверяемое состояние: заполнить недостающие разделы в указанных файлах, сохранить требования к структуре, опираться на правила кейса (SEC-1, API-1, REL-1, OUT-1, OBS-1) и результаты P1-01/P1-02.

Входы и источники
- Обязательные файлы: `practices/practice_01/README.md`, `CASE.md`, `TRAINING_PR.diff`, `Makefile` (в этой же папке), текущее содержимое `context.md`, `problem.md`, `prompts.md`.
- Язык: русский. Формат — Markdown.
- Не использовать web и внешние источники; опирайся только на предоставленные файлы.

Границы и запреты
- Не менять и не удалять: `TRAINING_PR.diff`, `CASE.md`, существующие разделы с корректной информацией.
- Не внедрять «размышления вслух» (Chain of Thought). Возвращай только готовый результат.
- Сохраняй иконки/блоки кода, от которых зависят проверки `Makefile`:
  - analysis.md: наличие раздела `## TO BE`;
  - product_management.md: блок с ```gherkin;
  - project_management.md: блок с ```mermaid;
  - adr.md: заголовок `## Архитектурная схема`.

Ожидаемые результаты (конкретные файлы и что в них должно появиться)
1) practices/practice_01/analysis.md
   - AS IS: кратко опиши текущий процесс от POST `/api/reviews` до ответа (где затыки сейчас: отсутствие лимита, таймаута, формата).
   - TO BE: диаграмма (Mermaid flowchart) со стадиями: проверка длины (API-1) -> маскирование секретов (SEC-1) -> вызов LLM с таймаутом (REL-1) -> сбор структурированного ответа (OUT-1) -> минимальное логирование (OBS-1).
   - Таблица «Разница»: минимум 3 строки (формат OUT-1, таймаут/контролируемые ошибки, лимит длины) с колонкой «Как проверим изменение».
   - Раздел «Как использовали AI»: цель, тип промпта, ссылка на строку в `prompts.md`, краткая самопроверка.

2) practices/practice_01/product_management.md
   - «Первый рабочий сценарий»: в формате When/Then.
   - Use case-таблица: актор, триггер, предусловия, основной результат, ошибка/отказ.
   - Два Gherkin-сценария: позитивный (валидный короткий diff) и негативный (diff > 20000 → 413).
   - «Как использовали AI» заполнен.

3) practices/practice_01/project_management.md
   - Таблица инкрементов (минимум 3): что делает человек / AI, проверка, зависимости. Инкременты: OUT-1, REL-1, API-1/SEC-1.
   - Диаграмма Ганта (Mermaid) с реальными датами/порядком (можно условные ближайшие даты).
   - «Как использовали AI» заполнен.

4) practices/practice_01/adr.md
   - Статус: accepted; дата: 2026-09-10; ответственные: Мустафин Тимур.
   - Контекст: ограничения и правила (SEC-1, API-1, REL-1, OUT-1, OBS-1).
   - Решение: как обеспечиваем лимит длины, редактирование секретов, таймаут и контролируемые ошибки, структурированный ответ и минимальное логирование.
   - Альтернативы: напр. обработка ошибок на уровне API vs сервиса; синхронный вызов vs очередь.
   - Последствия/главный риск и как проверим.
   - Архитектурная схема: Mermaid с компонентами (FastAPI, ReviewService, LLM provider, Logger/Obs).
   - «Как использовали AI» заполнен.

5) practices/practice_01/tests_unit.md
   - Минимум 3 строки: SEC-1 (редактор секретов), API-1 (валидация длины), OUT-1 (сборщик ответа формирует поля). Входы и ожидаемые результаты сформулировать конкретно.

6) practices/practice_01/tests_integration.md
   - Проверки: валидный POST `/api/reviews` -> 200 и структура OUT-1; таймаут LLM -> контролируемый ответ (без 5xx).

7) practices/practice_01/tests_load.md
   - Один сценарий с нагрузкой (порог по времени/стабильности) И/ИЛИ обоснование «пока не нужно» с условием включения.

8) practices/practice_01/tests_e2e.md
   - Три сценария: позитивный, негативный (>20000 → 413), граничный (ровно 20000).

9) practices/practice_01/prompts.md (минимальные уточнения)
   - Ничего не ломай. При необходимости добавь ссылку на пример JSON-результата P1-02 (короткий сниппет) отдельным файлом.

Требования к стилю и проверяемости
- Пиши кратко и по делу; избегай воды.
- Ставь ссылки на файлы/строки и правила, когда ссылаешься на evidence.
- В каждом содержательном файле заполни блок «Как использовали AI».
- Не выдумывай факты, не подтвержденные входами; если данных не хватает — явно укажи допущение.

Критерии приёмки (Definition of Done)
- Команда `make test` в `practices/practice_01/` проходит (включая grep на обязательные блоки).
- Выполнены пункты оценки из `README.md` Практики 1 для контента, а не кода.
- Все новые/измененные разделы согласованы с правилами `SEC-1`, `API-1`, `REL-1`, `OUT-1`, `OBS-1`.

Формат вывода (строго)
- Верни итог как набор секций по файлам:

```
File: practices/practice_01/analysis.md
<полное содержимое файла>

File: practices/practice_01/product_management.md
<полное содержимое файла>

... и т.д. для каждого редактируемого файла
```

- Не включай «поток мыслей», черновики и скрытую Chain of Thought. Только финальные тексты.
- Если файл уже заполнен и корректен — не переписывай его; измени только недостающие части.

Подсказки для самопроверки перед завершением
- Есть ли в `product_management.md` блок ```gherkin?
- Есть ли в `project_management.md` блок ```mermaid?
- Есть ли в `analysis.md` раздел `## TO BE`?
- В `adr.md` есть раздел `## Архитектурная схема` и она отражает реальные компоненты?
- В тестовых файлах таблицы без пустых строк и с конкретными ожиданиями?
- Не изменены ли случайно `TRAINING_PR.diff` и `CASE.md`?

Стартовая команда
- Начни с чтения `CASE.md`, `TRAINING_PR.diff`, `prompts.md` (P1-01/P1-02), затем заполни недостающие файлы по списку выше.
