# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| API-1: лимит 20 000 символов | Подсчёт Unicode-символов после JSON decode и до redaction | Строки длиной 19 999; 20 000; 20 001, включая многобайтовые символы | Первые два проходят; третий — ошибка лимита | планируемый unit-тест; ожидаем raise/False для 20 001 |
| Валидация содержимого | Пустой и пробельный diff | `""`, `"   "`, `"\n\t"` | Ошибка валидации; LLM не вызывается | планируемый unit-тест; ожидаемый HTTP-контракт 422 |
| SEC-1: redaction секретов | Замена токена/пароля/приватного ключа на [REDACTED] | Строки с `token=abc123`, `password: qwerty`, блок `-----BEGIN PRIVATE KEY-----` | В выходе нет исходных секретов; присутствует `[REDACTED]` | планируемый unit-тест; ожидаем замену по паттернам |
| OUT-1: максимум 3 риска | Ограничение длины списка рисков | Вход с 5 кандидатов | Возвращается не более 3 элементов | планируемый unit-тест; ожидаем len(risks) <= 3 |
| OUT-1: структура ответа | Формирование `summary/risks/checks` | Мок ответа LLM и маппер | Выходной dict содержит поля; каждый риск имеет file,line,evidence,risk | планируемый unit-тест; проверка ключей |
| REL-1: таймаут/исключение LLM | Обёртка вызова LLM | Функция, вызывающая исключение/задержку >10с | HTTP 502; `error.code=LLM_UPSTREAM_ERROR`; детали провайдера отсутствуют | планируемый unit-тест с мок LLM |
| OBS-1: безопасные логи | Конфигурация логирования | Объект события с diff/LLM-ответом | Логи не содержат diff/ответ модели; только метаданные | планируемый unit-тест; проверка записей логгера |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-02.
- Что проверили и исправили сами: привели проверки к правилам CASE.md; избавились от плейсхолдеров и неподтверждённых утверждений.
