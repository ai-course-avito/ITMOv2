# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| `ReviewService.review` формирует промпт с содержимым diff (TRAINING_PR.diff:20) | Что промпт, переданный в `llm.generate`, содержит подстроку `diff` и префикс "Review this pull request and find problems:" | Мок `LLM` с сохранением аргумента `prompt`; `diff = "some diff text"` | Мок зафиксировал вызов `generate` с prompt, содержащим `"some diff text"` | TRAINING_PR.diff:20 |
| `ReviewService.review` возвращает `{"comment": answer}` (TRAINING_PR.diff:22) | Что возвращаемое значение — словарь с единственным ключом `comment`, равным ответу мок-LLM | Мок `LLM.generate` возвращает `"ok"` | `review(...)` возвращает `{"comment": "ok"}` | TRAINING_PR.diff:19-22 |
| Отсутствие обработки ошибок `llm.generate` (TRAINING_PR.diff:21) | Что происходит, если `llm.generate` бросает исключение — сейчас оно пробрасывается без изменений | Мок `LLM.generate`, поднимающий `TimeoutError` | Текущее поведение (AS IS): исключение пробрасывается наружу из `review` — тест документирует проблему до фикса; после фикса ожидается контролируемый ответ вместо исключения | TRAINING_PR.diff:21 |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-03.
- Что проверили и исправили сами: тесты сформулированы только для поведения, которое явно видно в diff (формирование промпта, форма ответа, отсутствие обработки исключений); не добавлены проверки для функциональности, которой в diff нет (например, санитизация секретов), чтобы не выдавать желаемое поведение за текущее.
