# Unit-проверки

Проверяется каждый компонент отдельно, без сети и без внешнего LLM. Всё, что уходит наружу, заменено заглушкой.

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| `API-1` | Валидатор размера отклоняет слишком длинный вход | diff на 20001 символ | Отказ с HTTP 413, клиент LLM не вызван ни разу | `API-1` в [`CASE.md`](CASE.md) |
| `API-1` | Валидатор размера пропускает граничное значение | diff ровно на 20000 символов | Вход принят, обработка продолжается | `API-1`, проверка границы порога |
| `SEC-1` | Функция удаления секретов заменяет токен | Строка `token=ghp_ABC123DEF456` внутри diff | В результате `[REDACTED]`, остальной текст не изменён | `SEC-1`, строка `review_service.py:20` в [`TRAINING_PR.diff`](TRAINING_PR.diff) |
| `SEC-1` | Удаление секретов не портит структуру diff | diff с секретом внутри строки, начинающейся с `+` | Число строк и префиксы `+` и `-` сохранены | `SEC-1`, строка «Разница» в [`analysis.md`](analysis.md) |
| `REL-1` | Обёртка таймаута прерывает долгий вызов | Заглушка LLM, отвечающая через 11 секунд | Возвращается контролируемая ошибка, исключение наружу не выброшено | `REL-1` в [`CASE.md`](CASE.md) |
| `OUT-1` | Сборка ответа обрезает список рисков | Список из пяти подтверждённых рисков | В ответе ровно три элемента `risks` | `OUT-1` в [`CASE.md`](CASE.md) |
| `OUT-1` | Ответ не добирается до трёх рисков искусственно | Список из одного подтверждённого риска | В ответе один элемент `risks`, поля `summary` и `checks` заполнены | `OUT-1`, поле «Ошибка или отказ» в [`product_management.md`](product_management.md) |
| `QA-1` | Фильтр отбрасывает неподтверждённый риск | Два риска: один со ссылкой на строку diff, второй без evidence | В ответе остаётся только первый | `QA-1` в [`CASE.md`](CASE.md) |
| `OBS-1` | Запись журнала не содержит полезной нагрузки | Обработанный запрос с секретом внутри diff | В записи только `request_id`, длительность и статус | `OBS-1` в [`CASE.md`](CASE.md) |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): `P1-06`
- Что проверили и исправили сами: сверил, что каждая строка Evidence ссылается на реально существующее правило `CASE.md`, и что парные проверки границы 20000 символов не потерялись
