# Unit-проверки

Файл ведёт OpenCode. Обсудите с агентом содержание и проверьте предложенный diff. Все дополнения и исправления поручайте агенту в чате.

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Подтверждение |
|---|---|---|---|---|
| Формирование prompt | ReviewService.review формирует prompt с вставкой diff | diff = "X" | Вызов LLM.generate с префиксом "Review this pull request" и текстом diff | Мок LLM, assert аргумента |
| Прозрачная передача ответа | ReviewService.review возвращает comment из ответа LLM | LLM.generate -> "ok" | {"comment": "ok"} | Проверка значения |
| Типизация протокола | Метод generate имеет сигнатуру (str)->str | n/a | Соответствие протоколу | Статическая проверка/минимальный тест |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-03.
- Что проверил студент и какие исправления поручил агенту: подтвердил юнит-объекты и границы тестов.
