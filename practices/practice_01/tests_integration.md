# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| FastAPI handler → ReviewService | KeyError при отсутствии `payload["diff"]` | POST /api/reviews без ключа `diff` | AS IS: 500; TO BE: 422 | app/api.py:36-38 — прямой доступ к ключу |
| ReviewService → LLM | Исключение от LLM не перехватывается | Смоделировать generate() выбрасывает ошибку | AS IS: падение; TO BE: перехват и маппинг | app/review_service.py:19-22 — отсутствие try/except |
| Передача diff в промпт | Переполнение/инжекция | Передать большой/злонамеренный diff | AS IS: прямая вставка; TO BE: усечение/очистка | app/review_service.py:19-22 — прямая вставка строки |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-02
- Что проверили и исправили сами: Привязали каждый риск к строкам diff; зафиксировали AS IS и ожидаемое TO BE поведение раздельно.
