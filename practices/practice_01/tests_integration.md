# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| FastAPI endpoint ↔ валидатор запроса | Неверное тело запроса | POST `/api/reviews` без `diff` | 422 Unprocessable Entity | Код 422 |
| FastAPI endpoint ↔ лимитер длины | Слишком длинный diff | POST с `len(diff) = 20001` | 413 Payload Too Large | Код 413 |
| Маскирование ↔ вызов LLM | Утечка секретов в LLM | POST с токеном `ghp_...` и PEM | В промпте к LLM секреты заменены на `[REDACTED]` | Маркер `[REDACTED]` в переданном промпте |
| Клиент LLM ↔ таймаут | Висит дольше 10 секунд | Мок LLM со сном >10с | 504 Gateway Timeout | Код 504 |
| Клиент LLM ↔ обработка исключений | Провайдер бросает исключение | Мок LLM поднимает исключение | 502 Bad Gateway | Код 502 |
| Формирование ответа ↔ контракт OUT-1 | Неверная структура | Валидный diff | 200 и JSON с `summary`, `risks`≤3, `checks` | Схема OUT-1 валидируется |

## Как использовали AI

- Для чего: спроектировать интеграционные сценарии взаимодействия FastAPI, валидации, маскирования и клиента LLM.
- Тип промпта: Master Prompt v2
- Строка в [`prompts.md`](prompts.md): P1-04
- Что проверили и исправили сами: добавили сценарии 422, 413, 502, 504 и проверили наличие маркера `[REDACTED]` для SEC-1.
