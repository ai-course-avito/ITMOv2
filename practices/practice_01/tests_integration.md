# Integration-проверки

| Связь компонентов           | Что может сломаться                 | Как воспроизводим                                                                                                                              | Ожидаемый результат                                                                                        | Evidence                                                           |
| --------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| HTTP слой ↔ валидация входа | Нет 413 для >20000 символов         | Отправить POST /api/reviews с diff длиной 20001 символ                                                                                         | Ответ 413; тело с кратким описанием ошибки; без обращения к LLM                                            | API-1; проверка длины на входе; метрика из problem.md              |
| HTTP слой ↔ валидация входа | Неверная обработка ровно 20000      | POST /api/reviews с diff длиной 20000                                                                                                          | Ответ 200; корректный JSON по OUT-1                                                                        | API-1; boundary case                                               |
| Санитайзер ↔ LLM-клиент     | Утечка секретов в prompt            | POST с diff, содержащим токены/ключи                                                                                                           | Стаб LLM получает prompt, где секреты заменены на [REDACTED]; содержимое prompt не логируется              | SEC-1; инспекция prompt на стороне стаба, без логов содержимого    |
| LLM-клиент ↔ таймаут        | Зависание >10с без деградации       | Подменить LLM на стабильную задержку 11с                                                                                                       | HTTP 200; JSON с деградацией: summary указывает на timeout; risks=[], checks содержит шаги ручной проверки | REL-1; «контролируемый ответ» по context.md                        |
| Ответ сервиса ↔ схема       | Неструктурированный ответ           | Обычный запрос с небольшим diff                                                                                                                | Ответ соответствует OUT-1: валидный JSON, risks<=3, корректные типы                                        | OUT-1; схема из unit-проверок                                      |
| Ограничение risks           | Сервер пропускает >3 рисков из LLM  | Застабировать LLM, чтобы вернуть 5 рисков                                                                                                      | В ответе не более 3 рисков (обрезано/отфильтровано)                                                        | OUT-1; QA-1                                                        |
| Логирование ↔ безопасность  | Логи содержат diff/LLM payload      | Выполнить запрос и собрать логи за период                                                                                                      | Записаны только request_id, duration, status; отсутствуют diff и payload                                   | OBS-1; формат plain text допустим; поля на английском              |
| Валидация пустого входа     | Пустой diff принимается             | POST /api/reviews с пустой строкой/пробелами                                                                                                   | HTTP 400; код ошибки «empty_diff»; без обращения к LLM                                                     | Ссылка: tests_unit.md (раздел «Валидация входа») и это же правило в данном документе |
| Идентификация request_id    | Невозможно сопоставить запрос и лог | Выполнить запрос и проверить, что в логах есть уникальный request_id; если сервис возвращает его клиенту (заголовок/тело) — значения совпадают | В логах присутствует request_id; при наличии в ответе — коррелирует                                        | OBS-1; не требует изменения контракта ответа                       |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-05
- Что проверили и исправили сами:
  - -
