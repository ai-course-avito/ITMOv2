# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| `app/api.py` (`create_review`) → `app/review_service.py` (`ReviewService.review`) | Тело запроса без ключа `diff` вызывает необработанный `KeyError` вместо понятной ошибки (TRAINING_PR.diff:36-37) | `POST /api/reviews` с телом `{}` через тестовый клиент FastAPI | AS IS: 500 с трейсом `KeyError`; после фикса — 4xx с сообщением об обязательности поля `diff` | TRAINING_PR.diff:36-37 |
| `app/api.py` → `app/review_service.py` → `LLM` | Ключ `diff` присутствует, но имеет неверный тип (например, число), что не проверяется (TRAINING_PR.diff:36-37) | `POST /api/reviews` с телом `{"diff": 123}` | AS IS: значение попадает в f-string без ошибки типа на этом шаге, что скрывает проблему до вызова LLM; после фикса — 4xx на этапе валидации | TRAINING_PR.diff:20, 36-37 |
| `app/review_service.py` → внешний `LLM` | Исключение из `llm.generate` (таймаут/сбой) не перехватывается на уровне сервиса и всплывает до FastAPI как 500 (TRAINING_PR.diff:21) | `POST /api/reviews` с валидным `diff`, но с мок-реализацией `LLM`, поднимающей исключение (подмена зависимости `review_service` в тестах) | AS IS: клиент получает 500 без деталей; после фикса — контролируемый 5xx/4xx ответ с понятным сообщением | TRAINING_PR.diff:21 |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-03.
- Что проверили и исправили сами: сценарии ограничены связкой `api.py` ↔ `review_service.py` ↔ `LLM`, которая явно видна в diff; не добавлены сценарии для `app/dependencies.review_service`, так как его реализация вне разрешённых источников для этого запуска.
