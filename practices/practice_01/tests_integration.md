# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| FastAPI эндпоинт -> ReviewService -> мок LLM | Позитивный сценарий выдачи ответа | POST `/api/reviews` с валидным `diff` | 200 OK; тело `{ summary, risks[], checks[] }` | логи / тест-раннер |
| FastAPI эндпоинт -> лимит входа (API-1) | Diff длиной > 20000 | POST `/api/reviews` с `diff` длиной 20001 | 413 Payload Too Large, без включения diff в ответ | логи / тест-раннер |

## Как использовали AI

- Для чего: описать минимальные интеграционные сценарии позитив/негатив для текущего эндпоинта с учётом целевых правил.
- Тип промпта: master prompt (P1-03).
- Строка в [`prompts.md`](prompts.md): P1-03.
- Что проверили и исправили сами: синхронизировали статусы ошибок с правилами CASE, учли поведение FastAPI при валидации.
