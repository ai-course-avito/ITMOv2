PR_REVIEW:

Summary:
PR добавляет сервис ReviewService.review для формирования prompt и вызова LLM, а также эндпоинт POST /api/reviews, принимающий JSON и возвращающий {"comment": answer}. Функционально это замыкает путь: клиент → API → LLM → ответ.

Risks (<3):
app/api.py:37 + evidence: return review_service.review(payload["diff"]) + rule: Контракт входа эндпоинта требует, чтобы payload содержал ключ 'diff' со строковым значением (см. context.md: Формат входа и результата; Плохой пример указывает на 500 при отсутствии 'diff').

Checks:
- Негативный сценарий (нет 'diff'): запустить сервис, выполнить POST /api/reviews c body {}. Ожидаемо: KeyError и 500 (как в Плохом примере в context.md). Пример: curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8000/api/reviews -H 'Content-Type: application/json' -d '{}'
- Позитивный сценарий: POST /api/reviews с корректным body {"diff":"diff --git ..."}. Ожидаемо: 2xx и JSON {"comment":"..."}. Пример: curl -s -X POST http://localhost:8000/api/reviews -H 'Content-Type: application/json' -d '{"diff":"diff --git a/file b/file"}'
