Ключевые изменения и риски (по TRAINING_PR.diff):

- {app/api.py:36-37} - Потенциальный KeyError при обращении к `payload["diff"]` без валидации входа. Нарушение QA-1 при формировании риска подтверждено строкой diff.
- {app/review_service.py:20-22} - Diff передаётся внешнему LLM без удаления секретов, что нарушает SEC-1. Формирование prompt: вся строка diff включена целиком.
- {app/review_service.py:21} - Отсутствует timeout и обработка ошибок вызова LLM, что нарушает REL-1 (неуправляемые задержки/исключения).
- {app/api.py:35-37} - Нет проверки длины `diff` и возврата HTTP 413 для больших входов, что нарушает API-1.
- {app/review_service.py:22} - Ответ не соответствует OUT-1: возвращается `{"comment": ...}` вместо `summary`, `risks[]`, `checks[]` (макс. 3 риска с полями `file`, `line`, `evidence`, `risk`).

Вопросы и уточнения:

- Планируется ли слой валидации/санации входов и ограничение размера тела запроса на уровне FastAPI/middleware? Если да — нужен evidence в diff.
- Где реализуется контроль таймаута и обработка ошибок LLM — в адаптере клиента или здесь? В текущем diff этого нет.
- Требуется ли логирование по OBS-1 в этом сервисе? В diff нет логирования; не включено в риски без evidence.

Проверка и evidence:

- Сопоставлены строки diff: `app/review_service.py:19-22`, `app/api.py:35-38` с правилами из CASE.md: SEC-1, API-1, REL-1, OUT-1, QA-1. Все включённые риски подтверждены строками или правилами.
