# E2E-проверки

| Сценарий пользователя | Предусловия | Действие | Наблюдаемый результат | Evidence |
|---|---|---|---|---|
| Позитивный | Сервис запущен, эндпоинт `/api/reviews` доступен (TRAINING_PR.diff:35) | Клиент отправляет `POST /api/reviews` с телом `{"diff": "diff --git a/foo.py ..."}` | Клиент получает `200 OK` с телом `{"comment": "<текст ответа LLM>"}` (TRAINING_PR.diff:22) | TRAINING_PR.diff:19-22, 35-37 |
| Негативный | Сервис запущен | Клиент отправляет `POST /api/reviews` с пустым телом `{}` | AS IS: клиент получает `500` с необработанным `KeyError` вместо понятной ошибки; целевое поведение — `4xx` с сообщением, что поле `diff` обязательно | TRAINING_PR.diff:36-37 |
| Граничный | Сервис запущен | Клиент отправляет `POST /api/reviews` с телом, где `diff` — очень длинная строка (размер не ограничен кодом, TRAINING_PR.diff:20) | Поведение не определено кодом: возможен таймаут или ошибка на стороне LLM без контролируемой обработки (TRAINING_PR.diff:20-21) — требуется отдельная проверка после введения лимита длины | TRAINING_PR.diff:20-21 |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-03.
- Что проверили и исправили сами: граничный сценарий явно помечен как «поведение не определено» вместо того, чтобы выдумать конкретный лимит длины diff, которого нет в коде и который не подтверждён разрешёнными источниками для этого запуска.
