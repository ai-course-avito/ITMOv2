# E2E-проверки

| Сценарий пользователя | Предусловия                                                       | Действие                                           | Наблюдаемый результат                                                                                                            | Evidence                                 |
| --------------------- | ----------------------------------------------------------------- | -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| Позитивный            | Сервис запущен: POST /api/reviews; base URL http://localhost:8000 | Отправить реальный учебный diff (TRAINING_PR.diff) | HTTP 200; JSON соответствует OUT-1; summary на RU; risks<=3 c file,line,evidence,risk; checks — чеклист                          | OUT-1, QA-1 из context.md                |
| Негативный            | Сервис запущен                                                    | Отправить diff длиной 20001 символ                 | HTTP 413; тело с кратким описанием; без задержки                                                                                 | API-1; problem.md                        |
| Граничный             | Сервис запущен; LLM-стаб с задержкой 11с                          | Отправить валидный небольшой diff                  | HTTP 200 ≤10с; JSON деградации: summary указывает на timeout; risks=[]; checks содержит ручные шаги                              | REL-1; context.md (контролируемый ответ) |
| Безопасность          | Сервис запущен; включено логирование в файл                       | Отправить diff с тестовыми секретами               | В ответе и логах нет исходных секретов; в prompt/логах — [REDACTED]; лог содержит только request_id, duration, status (на англ.) | SEC-1, OBS-1 (результат RU; логи EN)     |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-05
- Что проверили и исправили сами:
  - Зафиксировали язык требований: пользовательский результат — RU; системные логи — EN
