# Контекст AS IS и Context Pack

## Продукт и команда сейчас

| Вопрос | Ответ | Источник или допущение |
|---|---|---|
| Какой продукт или сервис рассматриваем? | AI-помощник ревьюера PR | CASE.md |
| Кто им пользуется? | Ревьюеры/разработчики репозитория | Допущение по роли помощника из CASE.md |
| Как устроен текущий процесс? | FastAPI-сервис: POST /api/reviews принимает payload и проксирует diff в ReviewService, который формирует prompt и вызывает внешний LLM; есть /health | TRAINING_PR.diff |
| Где возникает задержка или ошибка? | Несоответствия правилам: SEC-1 (нет редакции секретов), API-1 (нет 413 на >20000 символов), REL-1 (нет таймаута/контролируемого ответа), OUT-1 (ответ не в требуемом формате) | TRAINING_PR.diff + CASE.md |
| Какие системы и команды участвуют? | FastAPI-приложение, внешний LLM; логирование с ограничениями (OBS-1) | TRAINING_PR.diff + CASE.md |

## Context Pack для первого рабочего сценария

### Факты и правила

- SEC-1: перед отправкой во внешний LLM из diff удаляются токены, пароли и приватные ключи.
- API-1: diff длиннее 20 000 символов отклоняется с HTTP 413.
- REL-1: внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в контролируемый ответ.
- OUT-1: ответ содержит summary, массив risks (максимум 3, с полями file, line, evidence, risk) и массив checks.
- SCOPE-1: сервис только советует; не пишет код и не выполняет действия в GitHub.
- QA-1: риск включается в ответ только если подтверждён строкой diff или правилом.
- OBS-1: в лог пишутся только request_id, длительность и статус; содержимое diff и ответа модели не логируется.

### Формат входа и результата

- Вход: diff как строка в запросе к POST /api/reviews.
- Результат: объект по OUT-1 с полями summary, risks[<=3], checks.

### Ограничения и запрещённые действия

- Нельзя отправлять секреты во внешний LLM (SEC-1).
- Отклонять запросы с diff > 20000 символов с HTTP 413 (API-1).
- Обеспечить таймаут 10 секунд и контролируемый ответ при ошибке LLM (REL-1).
- Соблюдать роль помощника (SCOPE-1) и не логировать содержимое (OBS-1).
- Включать только подтверждённые риски (QA-1).

### Хороший пример

app/review_service.py:16 — diff отправляется во внешний LLM без удаления секретов,
что нарушает SEC-1. Проверка: передать diff с тестовым token и убедиться,
что в prompt осталось [REDACTED].

### Плохой пример

Код можно улучшить. Добавьте тесты и обработку ошибок.

### Что пока неизвестно

- Требуются ли аутентификация и rate limiting для /api/reviews.
- Кто потребляет API и как именно используется результат OUT-1.
- Каким образом реализуется редакция секретов (механизм/паттерн).

## Как использовали AI

- Для чего: провести ревью TRAINING_PR.diff по правилам CASE.md и собрать Context Pack.
- Тип промпта: P1-01 — zero-shot; P1-02 — master prompt.
- Строка в [`prompts.md`](prompts.md): P1-01 и P1-02.
- Что проверили и исправили сами: сопоставили риски со строками diff и правилами (QA-1), соблюдение формата OUT-1 (<=3 риска), наличие воспроизводимых checks; выявили несоответствия SEC-1, API-1, REL-1, OUT-1.
