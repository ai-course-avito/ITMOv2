# Контекст AS IS и Context Pack

## Продукт и команда сейчас

| Вопрос | Ответ | Источник или допущение |
|---|---|---|
| Какой продукт или сервис рассматриваем? | REST API сервис для LLM-ревью PR-диффов | TRAINING_PR.diff (изменения в app/api.py, app/review_service.py) |
| Кто им пользуется? | Клиент, отправляющий JSON с полем "diff" в POST /api/reviews | Задание: AI-reviewer для PR diffs |
| Как устроен текущий процесс? | Клиент отправляет diff; сервис формирует prompt и вызывает LLM.generate; ответ LLM возвращается как {"comment": str} | Код из TRAINING_PR.diff |
| Где возникает задержка или ошибка? | Потенциальная ошибка при отсутствии ключа 'diff' в payload (KeyError) | app/api.py (+37): payload["diff"] |
| Какие системы и команды участвуют? | FastAPI-приложение, сервис ReviewService, зависимость LLM с методом generate(prompt)->str | TRAINING_PR.diff |

## Context Pack для первого рабочего сценария

### Факты и правила

- Добавлен метод `ReviewService.review(diff: str) -> dict[str, str]`, формирующий prompt и возвращающий `{"comment": answer}`.
- В API добавлен эндпоинт `POST /api/reviews`, принимающий `payload: dict` и вызывающий `review_service.review(payload["diff"])`.
- Правила репозитория для Context Pack (из CASE.md):
  - `SEC-1`: перед отправкой во внешний LLM из diff удаляются токены, пароли и приватные ключи.
  - `API-1`: diff длиннее 20 000 символов отклоняется с HTTP 413.
  - `REL-1`: внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в контролируемый ответ.
  - `OUT-1`: ответ содержит `summary`, массив `risks` и массив `checks`. В `risks` максимум три элемента с полями `file`, `line`, `evidence`, `risk`.
  - `SCOPE-1`: сервис только советует; он не пишет код и не выполняет действия в GitHub.
  - `QA-1`: риск включается в ответ, только если он подтверждён строкой diff или правилом репозитория.
  - `OBS-1`: в лог пишутся только `request_id`, длительность и статус. Содержимое diff и ответа модели не логируется.

### Формат входа и результата

- Вход: HTTP POST `/api/reviews` с JSON-телом `{"diff": "<строка diff>"}`. Длина diff <= 20 000 символов (API-1).
- Результат: JSON вида `{"summary": "...", "risks": [{"file": "...", "line": 0, "evidence": "...", "risk": "..."}], "checks": ["..."]}` (OUT-1).

### Ограничения и запрещённые действия

- Помощник только формирует рекомендации (SCOPE-1). Запрещены операции approve, merge, прямое редактирование кода.
- Запрещено передавать в LLM неотфильтрованные секреты (SEC-1).
- Запрещено логировать тело diff и ответ модели (OBS-1).
- Запрещено включать риски без подтверждения строкой diff или правилом (QA-1).

### Хороший пример

```text
app/review_service.py:20 — diff отправляется во внешний LLM без удаления секретов, что нарушает SEC-1. Проверка: передать diff с тестовым token и убедиться, что в prompt передано [REDACTED].
```

### Плохой пример

```text
Код можно улучшить. Добавьте тесты и обработку ошибок.
```

### Что пока неизвестно

- Какая конкретная реализация LLM подключена в `app.dependencies.review_service` на проде.
- Механизм сквозной авторизации клиентов сервиса в целевой инфраструктуре.

## Как использовали AI

- Для чего: ревью TRAINING_PR.diff и формирование артефактов P1-02.
- Тип промпта: master prompt (см. раздел Master Prompt v1 в prompts.md).
- Строка в [`prompts.md`](prompts.md): секция "Master Prompt v1" для P1-02.
- Что проверили и исправили сами: соответствие выводов evidence из diff, формат рисков (<3, формула file:line + evidence + rule), отсутствие placeholders.
