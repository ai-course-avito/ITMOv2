# Контекст AS IS и Context Pack

## Продукт и команда сейчас

| Вопрос | Ответ | Источник или допущение |
|---|---|---|
| Какой продукт или сервис рассматриваем? | AI-помощник ревью PR: FastAPI-сервис, который принимает diff, отправляет его во внешний LLM и возвращает комментарий | TRAINING_PR.diff |
| Кто им пользуется? | Разработчики, отправляющие PR на ревью; ревьюеры, принимающие решение | CASE.md |
| Как устроен текущий процесс? | POST /api/reviews → ReviewService.review(diff) → LLM.generate(prompt) → dict с комментарием | TRAINING_PR.diff:19-22, 35-37 |
| Где возникает задержка или ошибка? | Синхронный вызов LLM блокирует воркер; нет таймаута — ошибка LLM → необработанное исключение → HTTP 500; секреты из diff уходят в LLM без фильтрации | P1-01 результат |
| Какие системы и команды участвуют? | FastAPI (api.py), ReviewService (review_service.py), внешний LLM (Protocol LLM) | TRAINING_PR.diff |

## Context Pack для первого рабочего сценария

### Факты и правила

- `SEC-1`: перед отправкой diff во внешний LLM удаляются токены, пароли и приватные ключи
- `API-1`: diff длиннее 20 000 символов отклоняется с HTTP 413
- `REL-1`: внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в контролируемый ответ
- `OUT-1`: ответ содержит `summary`, массив `risks` (≤3, с полями `file`, `line`, `evidence`, `risk`) и массив `checks`
- `SCOPE-1`: сервис только советует; он не пишет код и не выполняет действия в GitHub
- `QA-1`: риск включается в ответ, только если он подтверждён строкой diff или правилом репозитория
- `OBS-1`: в лог пишутся только `request_id`, длительность и статус; содержимое diff и ответа модели не логируется

### Формат входа и результата

- Вход: diff (str) — строка из `git diff` или аналогичного инструмента
- Результат: JSON с полями `summary` (str), `risks` (массив ≤3 объектов с `file`, `line`, `evidence`, `risk`), `checks` (массив строк)

### Ограничения и запрещённые действия

- Запрещено: approve, merge, редактирование кода
- Запрещено: придумывать правила, которых нет в репозитории
- Запрещено: логировать содержимое diff или ответ модели (OBS-1)

### Хороший пример

```text
app/review_service.py:16 — diff отправляется во внешний LLM без удаления секретов,
что нарушает SEC-1. Проверка: передать diff с тестовым token и убедиться,
что в prompt осталось [REDACTED].
```

### Плохой пример

```text
Код можно улучшить. Добавьте тесты и обработку ошибок.
```

### Что пока неизвестно

- Какой LLM используется за Protocol (модель, провайдер, стоимость)
- Есть ли уже механизм секретов или ключей в продакшн-диффах
- Какой budget/timeframe на внедрение исправлений

## Как использовали AI

- Для чего: анализ TRAINING_PR.diff для идентификации нарушений правил репозитория
- Тип промпта: zero-shot (P1-01), master prompt (P1-02)
- Строка в [`prompts.md`](prompts.md): P1-01, P1-02
- Что проверили и исправили сами: перепроверили каждый риск по правилам SEC-1..OBS-1; убрали придуманные замечания без evidence
