# R.C.T.F.

- **Role:** Инженер по надёжности (SRE), отвечающий за нагрузочное тестирование сервиса ревью PR; пишет проверяемую техническую документацию с конкретными командами, а не общие рекомендации.
- **Context:** Артефакт tests_load.md описывает нагрузочные проверки сервиса из TRAINING_PR.diff (эндпоинты `/api/reviews` и `/health`). Правила из context.md: SEC-1 (маскирование секретов), API-1 (diff > 20 000 символов → HTTP 413), REL-1 (таймаут LLM 10 секунд, ошибка → контролируемый ответ), OUT-1 (формат ответа), SCOPE-1 (сервис только советует), QA-1, OBS-1 (в логах только request_id/длительность/статус, без тела diff). Текущий tests_load.md страдает 5 проблемами: нет рабочего скрипта и команды запуска; не указано, мокается ли LLM; пороги (p99<20мс, 12с) не обоснованы окружением/числом воркеров; нет сценария деградации при параллельной нагрузке и медленном LLM; не измеряются RAM/CPU.
- **Task:** Переписать tests_load.md, устранив все 5 проблем, не меняя уже согласованные пороги REL-1 (10с) и API-1 (20 000 символов) и не придумывая новых правил вне context.md.
- **Format:** Таблица со столбцами Сценарий | Окружение | Нагрузка | Допустимый предел | Деградация | Системные метрики | Evidence — по каждому из трёх исходных сценариев, плюс абзац об ограничениях MVP и раздел «Как использовали AI» со ссылкой на строку P1-02 в prompts.md. В Evidence — обязательно реальная команда запуска (k6/locust), не описание отчёта.

## Полный запрос

Role: Инженер по надёжности (SRE), отвечающий за нагрузочное тестирование сервиса ревью PR; пишет проверяемую документацию с конкретными командами, а не общие рекомендации.

Context: tests_load.md описывает нагрузочные проверки сервиса из TRAINING_PR.diff (`/api/reviews`, `/health`). Правила из context.md: SEC-1, API-1 (diff > 20 000 символов → 413), REL-1 (таймаут LLM 10с), OUT-1, SCOPE-1, QA-1, OBS-1. В текущем файле 5 проблем: нет рабочего скрипта и команды запуска; не указано, мокается ли LLM; пороги (p99<20мс, 12с) не обоснованы окружением/числом воркеров; нет сценария деградации при параллельной нагрузке и медленном LLM; не измеряются RAM/CPU.

Task: Ты должен скопипастить tests_load.md из practice_01 в rctf папку, там уже перепиши tests_load.md, устранив все 5 проблем. Не меняй пороги REL-1 (10с) и API-1 (20 000 символов). Не придумывай правил вне context.md.

Format: Таблица (Сценарий | Окружение | Нагрузка | Допустимый предел | Деградация | Системные метрики | Evidence) по каждому из трёх сценариев + абзац об ограничениях MVP + раздел «Как использовали AI» со ссылкой на строку P1-02 в prompts.md. Evidence — только реальная команда запуска (k6/locust), не описание отчёта.

## Что получили

Переписанный артефакт [`tests_load.md`](tests_load.md) в папке `rctf`:
- Устранены 5 проблем: добавлены скрипт `load/load_tests.js` и конкретные команды запуска k6 в колонку Evidence; зафиксировано мокирование LLM; пороги обоснованы параметрами окружения (FastAPI/Uvicorn, 4 воркера, 1 vCPU, 2GB RAM); добавлен сценарий деградации при таймауте mock-LLM (11 с > 10 с `REL-1`); добавлены метрики CPU и RSS RAM.
- Пороги `REL-1` (10с) и `API-1` (20 000 символов) сохранены без изменений.
- Раздел «Как использовали AI» ссылается на строку P1-02 в `prompts.md`.

## Что изменили в исходном артефакте

- Файл и раздел: `rctf/tests_load.md`, таблица сценариев, скрипты запуска и раздел «Как использовали AI».
- Изменение:
  1. Заменили абстрактный столбец «Что измеряем» на «Деградация» и «Системные метрики» (CPU/RAM).
  2. Добавили столбец «Окружение» с фиксацией параметров хоста (4 воркера, 1 vCPU, 2GB RAM) и статуса мока LLM.
  3. В Evidence заменили текстовые описания отчётов на исполняемые команды запуска `k6`.
  4. Добавили рабочий сценарий деградации при медленном LLM с проверкой таймаута по `REL-1`.
  5. Обновили ссылку в «Как использовали AI» на P1-02.
- Как проверили: проверка соответствия правилам context.md (`SEC-1`, `API-1`, `REL-1`, `OUT-1`, `SCOPE-1`, `QA-1`, `OBS-1`), проверка синтаксиса скрипта k6 и аргументов CLI.
- Что отклонили: предложения поднять таймаут `REL-1` выше 10с и изменить лимит `API-1` (20 000 символов), добавление внешних инструментов мониторинга (Prometheus/Grafana), не описанных в контексте.