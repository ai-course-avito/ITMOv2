[Полный запрос]
Ты — ведущий инженер по производительности. Перепиши practices/practice_01/tests_load.md в практический документ.
Сервис: POST /api/reviews → ReviewService.review(diff) → внешний LLM; REL-1 = p95 < 10 с и контролируемый ответ на таймаут; API-1 = 413 на diff > 20 000 символов; evidence — результаты замеров.
Требования:
1. Замени оправдание «нагрузка пока не нужна» одной строкой о пороге перехода к нагрузке (>10 req/мин или частые REL-1 timeout в prod → включаем нагрузку).
2. Таблица SLO/SLA по сценариям.
3. Ровно три сценария, для каждого: инструмент (k6/Locust), профиль (RPS/duration: warmup+ramp+steady), метрики (p95/p99, error rate, 5xx, timeouts), критерии pass/fail, evidence (k6 summary/графики). S1: <1K символов, 5–10 RPS, p95 <500 мс, err <1%; S2: ~20 000 символов, 2–5 RPS, p95 <10 с (REL-1), timeouts <1%; S3: задержка LLM, 1–2 RPS, 0% 500, таймауты контролируемо обрабатываются.
4. В evidence только результаты замеров — никаких ссылок на len(diff) или строки diff.
5. Исправь опечитку «Для чему» → «Для чего».
Не выдумывай метрики: если нет замеров — честный плейсхолдер + TODO в рисках.
Выведи только markdown-файл tests_load.md.