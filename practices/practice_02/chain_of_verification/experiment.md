# Chain of Verification

- Проверяемый черновик: «Для надёжности обязательна очередь; redaction гарантирует удаление всех секретов».

## Запрос на проверку

Составь независимые вопросы к двум утверждениям, ответь только по `CASE.md` и `TRAINING_PR.diff`, затем дай исправленный вариант. Скрытые рассуждения не показывай.

## Вопросы проверки и evidence

| Вопрос | Источник | Результат |
|---|---|---|
| Требует ли кейс очередь? | `CASE.md`, `REL-1` | Нет; требуется timeout 10 секунд и контролируемая ошибка |
| Можно ли гарантировать удаление всех секретов? | `CASE.md`, `SEC-1` | Требование есть, но полнота детектора не доказана |
| Где необработанный diff уходит в LLM? | `TRAINING_PR.diff`, 19–21 | Diff напрямую включён в prompt |

## Исправленный результат

Очередь не обязательна. Выбирается синхронный pipeline с timeout. Неполная redaction остаётся риском и проверяется набором секретов через fake LLM.

## Что изменили в исходном артефакте

- Файл и раздел: `../practice_01/adr.md`, «Альтернативы» и «Главный риск».
- Изменение: очередь помечена избыточной, гарантия заменена проверяемым риском.
- Что отклонили: утверждения «очередь обязательна» и «redaction удалит всё».
