# Chain of Verification

- Проверяемый черновик или утверждение: В коде TRAINING_PR.diff присутствуют нарушения четырёх правил проекта (SEC-1, API-1, REL-1, OUT-1).

## Запрос на проверку

```
Утверждение: В коде @practices/practice_01/TRAINING_PR.diff присутствуют нарушения четырёх правил проекта:
1. SEC-1: diff не очищается от секретов перед отправкой в LLM.
2. API-1: нет проверки длины diff (лимит 20 000 символов).
3. REL-1: нет таймаута 10 секунд и перехвата ошибок при вызове LLM.
4. OUT-1: возвращается {"comment": ...}, а не {summary, risks, checks}.

Проверь это утверждение, изучив файл @practices/practice_01/TRAINING_PR.diff.

Ответь в формате:
1. Выведи список вопросов для проверки (по одному на каждое правило).
2. Дай ответ на каждый вопрос (с цитатой строки из кода как evidence).
3. Сделай итоговый вывод: подтверждается ли исходное утверждение.
```

## Вопросы проверки и evidence

| Вопрос | Источник или проверка | Результат |
|---|---|---|
| Очищается ли diff от секретов (SEC-1)? | `TRAINING_PR.diff` строка 20 | Нет, diff передается напрямую: `prompt = f"Review this pull request and find problems:\n{diff}"` |
| Есть ли проверка длины diff (API-1)? | `TRAINING_PR.diff` строка 37 | Нет, длина не проверяется: `return review_service.review(payload["diff"])` |
| Обернут ли вызов в таймаут (REL-1)? | `TRAINING_PR.diff` строка 21 | Нет, таймаута и try/except нет: `answer = self.llm.generate(prompt)` |
| Возвращается ли структура (OUT-1)? | `TRAINING_PR.diff` строка 22 | Нет, возвращается плоский ответ: `return {"comment": answer}` |

## Исправленный результат

Исходное утверждение полностью подтверждено прямыми цитатами из кода diff. Текущая реализация `ReviewService` нарушает все четыре заявленных архитектурных и продуктовых правила.

## Что изменили в исходном артефакте

- Файл и раздел: `practices/practice_01/context.md` (раздел «Факты и правила»)
- Изменение: к каждому правилу (SEC-1, API-1, REL-1, OUT-1) добавлена явная ссылка на строку нарушения в `TRAINING_PR.diff`, подтверждающая обоснованность правила.
- Что отклонили: ничего не отклонили, модель чётко извлекла строки из файла.
