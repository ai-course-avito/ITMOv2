# E2E-проверки (Chain of Verification)

| Сценарий пользователя | Предусловия | Действие | Наблюдаемый результат | Evidence |
|---|---|---|---|---|
| Пользователь отправляет diff и получает ответ | Сервис запущен; доступен POST `/api/reviews`; LLM замокан | Отправить запрос POST `/api/reviews` с телом JSON `{ "diff": "diff --git a/x b/x\n+line" }` | 200 OK; ответ в формате JSON с полем `comment` типа string | HTTP 200; JSONPath `$.comment` существует и имеет тип string; реализация: TRAINING_PR.diff `app/review_service.py` строки 19–22 и `app/api.py` строки 35–38 |

## Как использовали AI

- Для чего: описать один сквозной пользовательский сценарий с проверяемыми и подтверждаемыми ожиданиями.
- Тип промпта: master prompt (P1-03).
- Строка в `prompts.md`: P1-03.
- Что проверили и исправили сами: выровняли формат ответа с реализацией из TRAINING_PR.diff; заменили размытое Evidence на конкретные статус и поле ответа.
