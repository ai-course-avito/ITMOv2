# ReAct

- Цель: переписать tests_load.md, закрыв 5 известных проблем (нет рабочего скрипта, не ясен мок LLM, необоснованные пороги, нет сценария деградации, не измеряются RAM/CPU), опираясь только на evidence из файлов проекта.
- Доступные входы: practices/practice_01/tests_load.md (источник, только для чтения), practices/practice_01/context.md, practices/practice_01/CASE.md, practices/practice_01/TRAINING_PR.diff, practices/practice_01/tests_integration.md (прецедент оформления границы REL-1).
- Разрешённые действия: читать перечисленные файлы; создать папку practices/practice_02/react/, если её нет; записать/переписать только practices/practice_02/react/tests_load.md.
- Запрещённые действия: редактировать или удалять что-либо в practices/practice_01/; редактировать код приложения (app/*); approve/merge; придумывать правила или пороги, которых нет в context.md/CASE.md; обращаться к реальному внешнему LLM или сети.
- Максимальное число шагов: 8.
- Условие остановки и вопроса человеку: если после чтения context.md/CASE.md для одной из 5 проблем не находится evidence или правила, на которое можно опереться (например, обоснование конкретного порога); если целевую папку practices/practice_02/react/ не удаётся создать; если возникает конфликт между указанными правилами. В этих случаях — остановиться и сообщить, что именно неясно, не додумывать самостоятельно.

## Запрос

Работай по циклу Thought → Action → Observation, не больше 8 шагов. Каждый Action должен быть реальным чтением/записью файла, а не предположением о его содержимом.

1. Thought: нужно скопировать источник. Action: скопировать practices/practice_01/tests_load.md в practices/practice_02/react/tests_load.md (создать папку при необходимости). Observation: зафиксировать, что копия создана.
2. Thought: нужны правила проекта. Action: прочитать practices/practice_01/context.md и practices/practice_01/CASE.md. Observation: выписать SEC-1, API-1, REL-1, OUT-1, SCOPE-1, QA-1, OBS-1.
3. Thought: нужен прецедент по границе REL-1. Action: прочитать practices/practice_01/tests_integration.md. Observation: зафиксировать, как там обоснован допуск 10.5с.
4. Thought: для каждой из 5 проблем нужно найти evidence и решение. Action: сопоставить каждую проблему с конкретным правилом/прецедентом из шагов 2–3. Observation: зафиксировать, для какой проблемы evidence найден, а для какой — нет.
5. Если для какой-то проблемы evidence не найден — остановиться на этом шаге и явно сообщить, что не хватает данных, вместо того чтобы придумывать обоснование.
6. Thought: все evidence собраны. Action: переписать таблицу сценариев в practices/practice_02/react/tests_load.md, добавив по каждой строке скрипт/команду, статус мока LLM, обоснование порога, сценарий деградации, системные метрики. Observation: зафиксировать итоговое содержимое файла.
7. Thought: нужно проверить, что порог REL-1 (10с) и API-1 (20 000 символов) не изменены. Action: сверить итоговый файл с context.md. Observation: подтвердить совпадение или найти расхождение.
8. Stop: вернуть итоговый tests_load.md и краткий список из 5 проблем с указанием, где именно и чем закрыта каждая.

## Наблюдаемые действия и результаты

| Шаг | Действие | Наблюдение | Решение продолжить или остановиться |
|---|---|---|---|
| 1 | Скопировать `practices/practice_01/tests_load.md` в `practices/practice_02/react/tests_load.md` (создать папку при необходимости) | Создана папка `practices/practice_02/react/` и записан файл `tests_load.md` идентичный исходному | Продолжить |
| 2 | Прочитать `practices/practice_01/context.md` и `practices/practice_01/CASE.md` | Выписаны правила: `SEC-1` (удаление секретов), `API-1` (diff > 20 000 знаков -> HTTP 413), `REL-1` (внешний LLM timeout 10с, контролируемый ответ), `OUT-1` (структура JSON), `SCOPE-1` (только советует), `QA-1` (риск подтвержден evidence), `OBS-1` (лог только request_id, длительности и статуса) | Продолжить |
| 3 | Прочитать `practices/practice_01/tests_integration.md` | Зафиксирован прецедент оформления границы `REL-1`: вызов mock LLM с задержкой 15с, замер длительности вызова `<= 10.5с` (таймаут 10с + допуск 0.5с на оверхед) и возврат HTTP 504 | Продолжить |
| 4 | Сопоставить каждую из 5 проблем с правилами и прецедентами | Evidence найден по всем 5 проблемам: 1) Скрипт/команды запуска (locust/k6 для эндпоинтов из `TRAINING_PR.diff`); 2) Мок LLM (изоляция внешних API, прецедент mock LLM из `tests_integration.md`); 3) Обоснование порогов (`REL-1` 10с + допуск 0.5с, `API-1` 20 000 символов); 4) Сценарий деградации (контролируемый HTTP 504 при таймауте); 5) Системные метрики RAM/CPU (проверка стабильности воркеров без OOM/утечек) | Продолжить |
| 5 | Проверка полноты evidence для остановки | Все необходимые данные и правила присутствуют в файлах проекта, остановка и запрос человеку не требуются | Продолжить |
| 6 | Переписать таблицу сценариев в `practices/practice_02/react/tests_load.md` | Таблица сценариев дополнена колонками скрипта/команды, статуса мока LLM, обоснования порогов, деградации и системных метрик | Продолжить |
| 7 | Сверить итоговый файл с `context.md` | Порог таймаута согласован с `REL-1` (10с + допуск 0.5с по прецеденту), длина diff строго в рамках `API-1` (20 000 символов). Расхождений нет | Продолжить |
| 8 | Stop | Итоговый `tests_load.md` сформирован, 5 проблем закрыты с опорой на evidence | Завершить (Stop) |

## Что изменили в исходном артефакте

- Файл и раздел: `practices/practice_02/react/tests_load.md`, таблица нагрузочных проверок и пояснительная секция.
- Изменение:
  1. Добавили точные команды запуска (`locust`) с headless-режимом, числом пользователей, временем и тегами сценариев (закрыта проблема отсутствия рабочего скрипта).
  2. Эксплицитно зафиксировали статус мока LLM для каждого сценария: `/health` и отсечка `API-1` не задействуют LLM; для `POST /api/reviews` обязателен локальный мок с контролируемой задержкой (1–2 с) без сетевых вызовов (закрыта проблема неясности мока).
  3. Обосновали пороги допустимого времени задержки: таймаут `REL-1` (10с) с допуском 0.5с по прецеденту `tests_integration.md` (время < 10.5с вместо произвольных 12с), а также лимит размера diff приведен строго к `API-1` (20 000 символов) (закрыта проблема необоснованных порогов).
  4. Добавили сценарий деградации при задержке мока LLM свыше 10с: срабатывание таймаута `REL-1` с контролируемым возвратом HTTP 504 вместо падения воркеров с кодом 500 (закрыта проблема отсутствия сценария деградации).
  5. Ввели мониторинг системных метрик (RAM/CPU): ограничение утилизации CPU (<40–70%) и контроль отсутствия утечек RSS памяти (RAM < 150–300 MB) при удержании соединений (закрыта проблема отсутствия замеров RAM/CPU).
- Как проверили:
  - Сверили правила и числовые пороги с `practices/practice_01/context.md` и `CASE.md` (`REL-1` = 10 секунд, `API-1` = 20 000 знаков).
  - Сверили допуск 10.5с с прецедентом интеграционного тестирования в `practices/practice_01/tests_integration.md` (строка 7).
  - Убедились, что таблица содержит все 5 целевых атрибутов для каждого сценария.
- Что отклонили:
  - Отклонили использование реальных запросов во внешний LLM (нарушение ограничений окружения и стоимости токенов).
  - Отклонили добавление новых сценариев сверх исходных трёх (`/health`, валидный diff, отсечка `API-1`), чтобы избежать избыточного переинжиниринга.
  - Отклонили изменение согласованных лимитов `REL-1` (10с) и `API-1` (20 000 символов).
