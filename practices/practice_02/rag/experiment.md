# RAG

- Вопрос к источникам: Какие интеграционные тесты необходимо написать для ReviewService, чтобы они покрывали требования из CASE.md и исправляли ошибки из TRAINING_PR.diff?

## Разрешённые источники

| Файл или документ | Зачем нужен | Какой фрагмент используем |
|---|---|---|
| `@practices/practice_01/CASE.md` | Извлечение бизнес-требований и правил для сервиса | Разделы "Требования к сервису" и "Ограничения" (SEC-1, API-1, REL-1, OUT-1) |
| `@practices/practice_01/TRAINING_PR.diff` | Поиск уязвимых мест в текущем коде для создания тест-кейсов | Функция `create_review` в `app/api.py` и `ReviewService.review` |

## Запрос

```
Вопрос: Какие интеграционные тесты необходимо написать для ReviewService, чтобы они покрывали требования из CASE.md и исправляли ошибки из TRAINING_PR.diff?

Разрешённые источники:
1. @practices/practice_01/CASE.md
2. @practices/practice_01/TRAINING_PR.diff

Ответь, перечислив 4 конкретных интеграционных тест-кейса. Каждый тест-кейс должен ссылаться либо на конкретное правило из CASE.md, либо на конкретную строку из TRAINING_PR.diff. Формат ответа — markdown-таблица:
| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Ссылка на источник |
```

## Ответ со ссылками на источники

Модель вывела markdown-таблицу из 4-х тестов:

1. **API -> ReviewService -> LLM**: Дифф с секретами уходит без очистки. Ожидаемый: `[REDACTED]`. Ссылка: `@practices/practice_01/CASE.md` (SEC-1).
2. **API -> ReviewService**: Ответ не соответствует контракту OUT-1. Ожидаемый: `summary, risks, checks`. Ссылка: `@practices/practice_01/TRAINING_PR.diff:22` (текущий код возвращает `comment`).
3. **API -> ReviewService**: Слишком длинный diff не отклоняется. Ожидаемый: `HTTP 413`. Ссылка: `@practices/practice_01/CASE.md` (API-1).
4. **ReviewService -> LLM**: Нет таймаута 10 сек. Ожидаемый: завершение за ~10с с контролируемым ответом. Ссылка: `@practices/practice_01/CASE.md` (REL-1).

## Что изменили в исходном артефакте

- Файл и раздел: `practices/practice_01/tests_integration.md`
- Изменение: таблица тест-кейсов была обновлена. Добавлена колонка «Ссылка на источник», в которой каждый тест-кейс явно ссылается на правило `CASE.md` или конкретную строку из `TRAINING_PR.diff`.
- Как проверили ссылки: Ссылка на `TRAINING_PR.diff:22` корректна (там `return {"comment": answer}`). Ссылки на правила `CASE.md` соответствуют реальности.
- Что отклонили как неподтверждённое: Ничего. Формат чётко соблюдён.
