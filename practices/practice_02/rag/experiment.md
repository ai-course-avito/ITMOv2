# RAG

- Вопрос к источникам: Какие из пунктов problems.md подтверждаются TRAINING_PR.diff и правилами context.md, а какие нужно перенести в «Открытые вопросы»?

## Разрешённые источники

| Файл или документ | Зачем нужен | Какой фрагмент используем |
|---|---|---|
| practices/practice_01/context.md | Правила OUT‑1, REL‑1, API‑1 | Разделы «Правила репозитория», «Формат входа и результата» |
| practices/practice_01/prompts.md | Контекст P1‑01/P1‑02 и их scope | Таблица и разделы P1‑01/P1‑02 |
| practices/practice_01/problems.md | Исходный список проблем | Все разделы |

## Запрос

```
Ответь, опираясь только на разрешённые источники (ссылки на разделы/строки):
1) Для каждого пункта из problems.md укажи, подтверждается ли он TRAINING_PR.diff или правилами context.md (QA‑1, OUT‑1 и т.п.).
2) Если подтверждается — дай ссылку (файл/строка/правило) и предложи 1–2 checks.
3) Если не подтверждается — классифицируй как «Открытый вопрос» и предложи проверку.
Верни два списка: confirmed и questions, со ссылками.
```

## Ответ со ссылками на источники

- Подтверждён риск «KeyError при отсутствии diff» — evidence: TRAINING_PR.diff (app/api.py: строки 35–38); checks: POST {} ⇒ 422.
- Неподтверждён тезис «нужна аутентификация» — переносим в «open_questions» JSON; suggested checks: изучение требований, сценарии злоупотребления.

### Исправленный артефакт (P1‑02 Result JSON)

```json
{
  "summary": "Добавлен POST /api/reviews и метод ReviewService.review с вызовом внешнего LLM. Ввод не валидируется, промпт формируется из сырого diff, ответ неструктурирован.",
  "risks": [
    {
      "file": "app/api.py",
      "line": 36,
      "evidence": "def create_review(payload: dict) -> dict[str, str]:\n    return review_service.review(payload[\"diff\"])",
      "risk": "Невалидированный body и доступ к payload[\"diff\"] без проверки → возможен KeyError и 500 вместо 4xx.",
      "checks": ["POST {} ⇒ 422"]
    }
  ],
  "open_questions": [
    {
      "question": "Нужна ли аутентификация и rate limiting?",
      "suggested_checks": ["Проверка требований продукта", "Анализ сценариев злоупотребления"]
    }
  ]
}
```

## Что изменили в исходном артефакте

- Файл и раздел: practices/practice_01/prompts.md — «P1‑02 Result (OUT‑1)» JSON.
- Предложение изменения: перенести неподтверждённые пункты в «open_questions» JSON; для подтверждённых — добавить evidence и checks.
- Как проверили ссылки: сопоставили с TRAINING_PR.diff, prompts.md P1‑01/P1‑02 и context.md.
- Что отклонили как неподтверждённое: любые новые утверждения без источников.
- Примечание: исходный файл Практики 1 не изменяли; изменение зафиксировано в этом эксперименте.
