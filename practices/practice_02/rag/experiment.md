    Артефакт Практики 1: practices/practice_01/tests_load.md — раздел «Нагрузочные сценарии»

    Что хотим улучшить: переписать свободный текст нагрузочных проверок в воспроизводимые Gherkin-сценарии

Запрос

Сконвертируй нагрузочные проверки для tests_load.md из свободного текста в Gherkin.
Используй Given/When/Then, на русском, минимизируй риторику.
Выравняй с правилами CASE.md (SEC-1, API-1, REL-1, OUT-1) и метриками из problem.md.
Retrieval-Augmented Generation

    Источники для извлечения:

        practices/practice_01/tests_load.md — целевой раздел «Нагрузочные сценарии».

        CASE.md — правила SEC-1, API-1, REL-1, OUT-1.

        problem.md — метрики (latency, RPS, error rate, timeouts, ресурсы).

        practices/practice_01/ — существующие Gherkin-сценарии как эталон стиля.

    Извлечение: выпиши дословно:

        фрагменты свободного текста нагрузочных проверок;

        формулировки правил SEC-1, API-1, REL-1, OUT-1;

        метрики и пороги из problem.md;

        2–3 примера Gherkin из репозитория (они заменяют few-shot).

    Связывание: для каждого фрагмента нагрузочного текста укажи, какое правило и какая метрика к нему относятся.

    Генерация: на основе связанных фрагментов и извлечённых эталонов собери Gherkin-сценарии.

Формат вывода:

    Блок «Retrieved context» (цитаты с указанием файла и раздела).

    Блок «Mapping» (фрагмент → правило → метрика).

    Блок «Gherkin» (итоговые сценарии).

    Блок «Gaps» (чего не хватило в источниках).