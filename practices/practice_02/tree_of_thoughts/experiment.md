    Артефакт Практики 1: practices/practice_01/tests_load.md — раздел «Нагрузочные сценарии»

    Что хотим улучшить: переписать свободный текст нагрузочных проверок в воспроизводимые Gherkin-сценарии

Запрос (Tree of Thoughts)

Сконвертируй нагрузочные проверки для tests_load.md из свободного текста в Gherkin.
Используй Given/When/Then, на русском, минимизируй риторику.
Выравняй с правилами CASE.md (SEC-1, API-1, REL-1, OUT-1) и метриками из problem.md.

Решай задачу как дерево мыслей:

    Корень: исходный свободный текст нагрузочных проверок.

    Ветвление (3–4 ветки): предложи альтернативные стратегии декомпозиции, например:

        по типу нагрузки (RPS, latency, concurrency, soak);

        по правилам CASE.md (SEC-1, API-1, REL-1, OUT-1);

        по метрикам problem.md;

        по типам отказов (timeout, 5xx, деградация, отказ зависимостей).

    Оценка веток: для каждой ветки укажи плюсы, минусы и покрытие правил/метрик.

    Выбор: выбери 1–2 лучшие ветки (или их комбинацию) с обоснованием.

    Развёртка: внутри выбранной ветки сгенерируй Gherkin-сценарии.

    Обратный ход: если обнаружены пробелы (непокрытые правила/метрики), вернись на шаг 2 и добавь ветку.

Формат вывода:

    Схема дерева (текстом): Root → Branch → Leaf.

    Оценка веток таблицей: Ветка | Плюсы | Минусы | Покрытие.

    Финальный Gherkin.

    Блок «Непокрытые элементы» (если есть).

Что изменилось по сравнению с прошлой версией:

    убраны блоки «Примеры / Хороший результат / Плохой результат» из RAG, RCTF, ReAct, ToT;

    в CoV оставлен только один эталонный пример (нужен для Q6 — проверки формата);

    в RAG добавлена явная отсылка к репозиторию как источнику эталонов (замена few-shot);

    в RCTF добавлена строка «Язык: русский» и уточнение формата тела сценария.

Если нужно — сведу все пять в один мета-промпт с переключателем technique: cov|rag|rctf|react|tot и общим форматом вывода.