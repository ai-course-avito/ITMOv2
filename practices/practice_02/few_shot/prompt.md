Использование: перед запуском подставьте значения в блок Inputs и, при необходимости, поменяйте предпочитаемый инструмент (k6/Locust). Метапромпт заставляет модель отдать ровно один markdown-файл с тремя сценариями, целевыми метриками, SLO/SLA, критериями прохождения и evidence.
[ROLE]
Ты — ведущий инженер по производительности. Пишешь рабочую документацию на русском языке для tests_load.md. Твой документ:
- Строго практический: конкретные сценарии, параметры профиля нагрузки, целевые SLO/SLA и критерии прохождения.
- Без оправданий и деклараций «нагрузка пока не нужна».
- Содержит проверяемые метрики: p95/p99, error rate, timeouts, 5xx, а также ссылки на evidence (сводки k6/Locust, графики).
- Без некорректного evidence: никаких ссылок на «len(diff)» и т.п. Нужны итоги замеров (сводки/графики).
- Пишется грамотно: «Для чего», не «Для чему».

[INPUTS]
Вставь эти значения в нужные места документа (если чего-то нет — оставь четкий плейсхолдер и не выдумывай):
- product_name: <Название продукта/сервиса>
- base_url: <URL стенда/окружения>
- endpoint_under_test: <Основной endpoint под нагрузкой, например POST /api/v1/review>
- llm_latency_toggle: <Как включается/эмулируется задержка LLM: фича-флаг/заглушка/параметр>
- data_shape_small: <Описание small diff: <1K символов, типовые поля, пример генерации входных данных>>
- data_shape_large: <Описание large diff: ~20 000 символов, как формируется, граничные поля>
- auth: <Схема аутентификации/заголовки, если нужны>
- tool: k6 (предпочтительно) или Locust
- duration_defaults: { warmup: 2-3 мин, steady: 10-15 мин, ramp: 2-5 мин } — при необходимости скорректируй
- REL_1_ref: REL-1 — требование: p95 < 10 c для граничного diff
- monitoring: <Как собираются метрики ресурсов (CPU/RAM/GC), если доступно; иначе пропиши как «n/a» и укажи TODO-интеграцию>

[OUTPUT FORMAT]
Сгенерируй один markdown-файл со структурой:
1. Назначение (Для чего)
   - Коротко: цель документа, что именно нагружаем, что считаем успешным (SLO/SLA обобщенно).
2. Область и система под тестом
   - product_name, base_url, endpoint_under_test, оговорки/исключения.
3. Метрики и цели (SLO/SLA)
   - Общие определения: p95/p99, error rate, 5xx, timeouts, опционально ресурсы.
   - Таблица сводных целей по сценариям (см. ниже).
4. Инструменты и методика
   - tool, как запускается, как собираются метрики/отчеты, какая длительность по умолчанию.
5. Сценарии
   5.1. S1: Малый diff (<1K символов)
       - Профиль: 5–10 RPS (например, ramp 2 мин до 5 RPS, steady 10 мин 8 RPS), p95 < 500 мс, error rate < 1%
       - Данные: data_shape_small
       - Длительность: укажи warmup/ramp/steady
       - Метрики: перечисли p95/p99, error rate, 5xx, timeouts
       - Критерии прохождения: чёткие пороги, «pass/fail»
       - Evidence: что приложить (сводка k6/Locust, графики), пример ожидаемой сводки
   5.2. S2: Пограничный diff (~20 000 символов)
       - Профиль: 2–5 RPS, p95 < 10 с (REL-1), timeouts < 1%
       - Данные: data_shape_large
       - Длительность: warmup/ramp/steady
       - Метрики: p95/p99, error rate, 5xx, timeouts
       - Критерии прохождения: привязка к REL_1_ref
       - Evidence: сводка/графики, пример
   5.3. S3: Задержка LLM
       - Профиль: 1–2 RPS, акцент на корректную обработку timeouts, 0% 500
       - Как включается задержка: llm_latency_toggle
       - Метрики: p95/p99, error rate, 5xx=0, timeouts контролируемые
       - Критерии прохождения
       - Evidence
6. План запуска и воспроизводимость
   - Команды запуска для tool (k6/Locust), переменные окружения, как подставлять base_url, auth.
   - Где сохраняются отчеты и как их прикладывать.
7. Привязка к требованиям и риски
   - Сопоставление с REL_1_ref, список рисков/долгов (например, отсутствует мониторинг CPU — план интеграции).
8. Приложения (опционально)
   - Шаблон команд, ссылки на дашборды, пример структуры отчетов.

[HARD CONSTRAINTS]
- Три сценария строго соответствуют целям:
  - S1: <1K символов: 5–10 RPS, p95 < 500 мс, error rate < 1%.
  - S2: ~20 000 символов: 2–5 RPS, p95 < 10 с (REL-1), timeouts < 1%.
  - S3: задержка LLM: 1–2 RPS, timeouts корректно обрабатываются, 0% 500.
- Для каждого сценария явно указать: инструмент, профиль нагрузки, длительность, метрики (p95/p99, error rate, 5xx, timeouts), критерии прохождения, evidence (сводка/графики).
- Никаких ссылок на «len(diff)» и прочие нефункциональные доказательства — только результаты замеров.
- «Для чего» — корректная формулировка. Следить за орфографией.
- Таблица сводных SLO/SLA присутствует.
- Не добавлять разделы «почему нагрузка пока не нужна».

[STYLE]
- Короткие абзацы, чек-листы и таблицы, где уместно.
- Русский язык, нейтрально-деловой тон.
- Четкие пороги и «pass/fail» без размытых формулировок.
- Без «TODO» в основных критериях; «TODO» допускается только в рисках/плане улучшений.

[FEW-SHOT EXAMPLES]

Пример 1 — как оформлять сценарий (k6):
Вход:
- Название: S1: Малый diff
- Профиль: 8 RPS steady 10 мин, ramp 2 мин
- Порог: p95 < 500 мс, error rate < 1%

Выход (фрагмент):
### S1: Малый diff (<1K символов)
- Профиль: ramp 2 мин → steady 10 мин на 8 RPS (диапазон 5–10 RPS)
- Инструмент: k6
- Данные: data_shape_small
- Метрики: p95, p99, error rate, 5xx, timeouts
- Критерии прохождения:
  - p95 < 500 мс
  - error rate < 1%
  - 5xx = 0
- Evidence:
  - k6 summary (пример):
    - http_reqs: 4800
    - http_req_duration p(95): 430ms, p(99): 780ms
    - http_req_failed: 0.6%
    - 5xx: 0, timeouts: 0
  - График latency p95/p99 (ссылка/путь к файлу)

Пример 2 — как ссылаться на REL-1 и фиксировать timeouts (Locust):
Вход:
- Название: S2: Пограничный diff
- Профиль: 2–5 RPS
- Порог: p95 < 10 с (REL-1), timeouts < 1%

Выход (фрагмент):
### S2: Пограничный diff (~20 000 символов)
- Профиль: 2–5 RPS, steady 12 мин, ramp 3 мин
- Инструмент: Locust
- Данные: data_shape_large
- Метрики: p95, p99, error rate, 5xx, timeouts
- Критерии прохождения (REL-1):
  - p95 < 10 с
  - timeouts < 1%
  - error rate < 1%, 5xx = 0
- Evidence:
  - Locust stats (пример): p95=9.2s, p99=12.1s, failures=0.8%, timeouts=0.6%
  - Скриншот графика RPS/latency

[CHECKLIST BEFORE OUTPUT]
- Присутствуют 3 сценария с указанными целевыми метриками и профилями.
- У каждого сценария есть: инструмент, длительность, метрики, критерии прохождения, evidence.
- Есть сводная таблица SLO/SLA.
- Нет упоминаний «len(diff)» и оправданий «нагрузка не нужна».
- «Для чего» написано без ошибок.
- Документ самодостаточен для запуска и проверки результатов.

[FINAL INSTRUCTION]
Выведи только готовый markdown-документ tests_load.md согласно OUTPUT FORMAT и HARD CONSTRAINTS. Без префиксов/пояснений. выполни метапромпт для few-shot меняй файлы только few_shot/experiment.md, few_shot/test_load.md